class Constants {
  constructor(
    public readonly THEME_ID = '@display:theme-uuid',
    public readonly THEME_LIGHT_ID = '@display:theme-light',
    public readonly THEME_DARK_ID = '@display:theme-dark'
  ) {}
}

export default new Constants();
